<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.thinkpad.rxjavaoperator">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name=".MainActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

        <activity android:name=".operator.Activity_Create"/>
        <activity android:name=".operator.Activity_Just"/>
        <activity android:name=".operator.Activity_FromArray"/>
        <activity android:name=".operator.Activity_FromCallable"/>
        <activity android:name=".operator.Activity_FromFuture"/>
        <activity android:name=".operator.Activity_FromIterable"/>
        <activity android:name=".operator.Activity_Defer"/>

    </application>

</manifest>